# Skip Puppeteer's Chromium download during npm install.
# This keeps the Railway/Docker image under 500 MB.
# PDF generation will gracefully skip if Chrome is unavailable;
# summaries and emails still work normally.
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
puppeteer_skip_chromium_download=true
